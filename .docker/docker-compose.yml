version: "3.9"

services:
  web:
    build:
      context: ..
      dockerfile: .docker/Dockerfile
    env_file:
      - ../.env.local
    ports:
      - "8000:8000"
    restart: unless-stopped
    networks:
      - gis_centralize_db

networks:
  gis_centralize_db:
    external: true